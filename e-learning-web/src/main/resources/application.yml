server:
  port: 80

spring:
  application:
    name: e-learning-web
  cloud:
    config:
      discovery:
        enabled: true
        
eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false #set to false to not send the connection request each time (for instance when running a test case that doesn't need eureka)
    fetchRegistry: false #set to false to not send the connection request each time (for instance when running a test case that doesn't need eureka)
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
      
management:
  context-path: /actuator
  security:
    role: ADMIN # ADMIN is the default role, but we explicitly set it. 
      

      
spring.social.facebook.app.id: xxxxxxxxxxxxxxxxxxxxxxx
spring.social.facebook.app.secret: xxxxxxxxxxxxxxxxxxxxxxx

spring.social.twitter.consumer.key: xxxxxxxxxxxxxxxxxxxxxxx
spring.social.twitter.consumer.secret: xxxxxxxxxxxxxxxxxxxxxxx

spring.social.linkedin.app.id: xxxxxxxxxxxxxxxxxxxxxxx
spring.social.linkedin.app.secret: xxxxxxxxxxxxxxxxxxxxxxx